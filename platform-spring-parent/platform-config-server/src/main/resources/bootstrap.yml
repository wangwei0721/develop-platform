# servlet 相关
server:
  port: 3333
#  向eureka注册时，不设置context-path，否则无法识别
#  servlet:
#    context-path: /CONFIG-SERVER
# spring 相关
spring:
  application:
    name: CONFIG-SERVER
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          # search-locations: classpath:/config
          search-locations: /Users/wangwei/IdeaProjects/example/develop-platform/platform-spring-parent/config
